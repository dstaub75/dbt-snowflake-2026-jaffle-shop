macros:
- name: cents_to_dollars
  description: A macro that converts dollars do cents and round the under input decimal places, or 2 if none is provided
  arguments:
    - name: column_name
      type: string
      description: the name of the column you want to convert
    - name : decimals
      type : integer
      description: the number of decimals places, default to 2